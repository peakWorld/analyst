/**
 * TODO
 * 1. 检测已修改的文件, 输出对应的最外层路由地址 ✅
 * 2. 页面间的跳转关系(携带参数?)
 * 3. 检测(移除?)未使用的代码文件
 * 4. 检测、输出页面中的请求接口(包含参数?)
 * 5. 检测可复用组建(入参), 不包含框架组建 => [单、双文件组建]
 * 6. 检测css => [剔除无效样式\提取重复样式]
 * 7. 增量检测
 */

import './callTool-fe';
